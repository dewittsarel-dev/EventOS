import{render,screen}from'@testing-library/react';import{describe,expect,it}from'vitest';import DocumentsPage from'./page';describe('DocumentsPage',()=>{it('indexes evidence through clear workspace relationships',()=>{render(<DocumentsPage/>);expect(screen.getByText('Event definition and design')).toBeInTheDocument();expect(screen.getByText('Supplier and purchasing records')).toBeInTheDocument();expect(screen.getByText(/everyone sees the same approved record/)).toBeInTheDocument()})});
