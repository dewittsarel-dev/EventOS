import{render,screen}from'@testing-library/react';import{describe,expect,it,vi}from'vitest';import ActivityPage from'./page';vi.mock('../../components/app-shell/session-context',()=>({useAppSession:()=>({session:{token:'t',baseUrl:'http://localhost:3001',organizationId:'o'}})}));vi.mock('../../lib/dashboard-api',()=>({getDashboardOverview:vi.fn().mockResolvedValue({attention:{status:'NeedsAttention',items:[{id:'a',title:'Approve supplier quote',explanation:'Quote awaits human review',actionLabel:'Review quote',actionHref:'/quotations/q',source:'Commercial'}]},myTasks:{overdue:[],dueToday:[{id:'t',title:'Confirm venue',priority:'High',status:'Todo'}],dueThisWeek:[]},recentActivity:[{type:'Event',action:'Updated',subject:'Spring Wedding',occurredAt:'2026-08-08T10:00:00Z'}]})}));describe('ActivityPage',()=>{it('combines attention, tasks and activity without automating approvals',async()=>{render(<ActivityPage/>);expect(await screen.findByText('Approve supplier quote')).toBeInTheDocument();expect(screen.getByText('Confirm venue')).toBeInTheDocument();expect(screen.getByText(/no approval or action is performed automatically/i)).toBeInTheDocument();expect(screen.getByRole('link',{name:'Review quote'})).toHaveAttribute('href','/quotations/q')})});
