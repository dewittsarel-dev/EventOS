import { describe, expect, it, vi } from 'vitest'; import { approveExecutionGoLive, changeExecutionTaskStatus, createExecutionTask } from './event-execution-workspace-api';
describe('execution API',()=>{it('keeps task evidence and go-live as explicit actions',async()=>{const fetchMock=vi.fn().mockResolvedValue({ok:true,status:200,json:async()=>({})});vi.stubGlobal('fetch',fetchMock);const o={baseUrl:'http://localhost:3001',token:'token'};await createExecutionTask(o,'org','event',{title:'Build stage',completionCriteria:'Stage signed off'});await changeExecutionTaskStatus(o,'org','event','task','Completed','Photo and supervisor sign-off');await approveExecutionGoLive(o,'org','event');expect(fetchMock).toHaveBeenCalledTimes(3);expect(fetchMock.mock.calls[1][1].body).toContain('completionEvidence');expect(fetchMock.mock.calls[2][0]).toContain('/go-live')})});
